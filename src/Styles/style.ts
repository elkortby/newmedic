import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	centerPage: {
		flex: 1,
		alignItems: 'center',
		justifyContent: 'center',
	},
	fullSize: {
		width: "100%",
		// height: "100%"
	}
})

export { styles }